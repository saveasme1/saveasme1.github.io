const fs = require("fs");
const bm = fs.readFileSync("board-meta.js", "utf8");
const i = bm.indexOf("function setupContentClamp");
console.log(bm.slice(i, i + 1200));
console.log("\n--- landing openDetail ---");
const lb = fs.readFileSync("landing-boards.js", "utf8");
const j = lb.indexOf("async function openDetail");
console.log(lb.slice(j, j + 900));
console.log("\n--- html editor renderSafe ---");
const he = fs.readFileSync("html-editor.js", "utf8");
const k = he.indexOf("renderSafe");
console.log(he.slice(k, k + 600));
console.log("\n--- css clamp ---");
const css = fs.readFileSync("landing.css", "utf8");
const a = css.indexOf("is-clamped");
console.log(css.slice(a - 80, a + 500));
console.log("\n--- dialog css ---");
const b = css.indexOf(".board-dialog");
console.log(css.slice(b, b + 900));